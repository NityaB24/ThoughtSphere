<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thought-Sphere</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-5 sm:p-10 text-white">
        <nav class="flex flex-col sm:flex-row justify-end gap-2 sm:gap-10 border-2 border-zinc-800 p-2 rounded-xl mb-4 text-sm sm:text-xl">
            <a href="/profile" class="text-center sm:text-left">Profile</a>
            <a class="px-3 py-1 bg-red-500 rounded-md text-sm inline-block text-center sm:text-left" href="/logout">Logout</a>
        </nav>
        <div class="flex flex-col sm:flex-row items-start gap-3">
            <h1 class="text-2xl sm:text-3xl tracking-tighter mb-3 text-center sm:text-left">Welcome to <span class="text-[#cdea66]"> Thought-Sphere </span></h1>
        </div>
    
        <div class="posts mt-10 sm:mt-20">
            <h3 class="text-zinc-500 text-center sm:text-left">All Posts</h3>
            <div class="postcontainer mt-5 flex flex-col items-center sm:items-start">
                <% post.reverse().forEach(function(post){ %>
                    <div class="post w-full sm:w-2/3 mb-5 rounded-md border-2 border-zinc-800 bg-zinc-800 p-4">
                        <h4 class="text-[#cdea66] mb-2">@<%=post.user.username%></h4>
                        <p class="text-sm tracking-tight"><%= post.content%></p>
                        <small class="mt-5 inline-block"><%= post.likes.length%> Likes</small>
                        <div class="btns flex gap-4 mt-2">
                            <a class="text-red-500" href="/likes/<%=post._id%>">
                                <%= post.likes.indexOf(post.user.email) ===-1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-500" href="/edit/<%=post._id%>">Edit</a>
                        </div>
                    </div>
               <% }) %>
            </div>
        </div>
    </div>
</body>
</html>
